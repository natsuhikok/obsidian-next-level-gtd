import { describe, it } from 'vitest';

// NextActionsFilter は FilterSelection, ContextClassifier, NextActionsQuery に分割されました。
// テストは FilterSelection.test.ts, ContextClassifier.test.ts, NextActionsQuery.test.ts を参
describe('NextActionsFilter', () => {
	it.todo(
		'FilterSelection.test.ts, ContextClassifier.test.ts, NextActionsQuery.test.ts に移行済み',
	);
});
